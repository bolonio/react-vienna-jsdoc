/**
 * This is a utility function that returns the name of the elements to be generated.
 *
 * @format
 * @param {string} name - The name of the elements to be generated.
 * @returns {string} The name of the elements to be generated.
 */

export const getAllElements = (name: string) => {
  return name;
};
